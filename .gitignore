# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# Local secrets — Perforce credentials for testing. Never commit.
.env
.env.*
!.env.example

# Runtime data — holds live Perforce tickets (sessions.db), the audit
# log, per-user server-side workspaces, and the search index. Never commit.
/data/

# Internal working docs — kept in Perforce, NOT for the public GitHub
# mirror. They leak the live tailnet URL, real depot paths, and an
# unpatched-weakness list (attacker playbook). Never publish.
/HANDOFF.md
/LESSONS.md
/docs/code-review-secaudit-redteam.md

# OS / editor
.DS_Store
Thumbs.db
*.swp
*~
