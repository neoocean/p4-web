# Copy to .env for local testing. .env is gitignored — never commit real
# credentials. These are only read by local test/dev harnesses, NOT by the
# server itself (the app authenticates each web user with their own login).
id=your_perforce_user
password=your_perforce_password_or_ticket
